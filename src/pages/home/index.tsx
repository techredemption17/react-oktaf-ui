import { Card } from 'antd'
import React, { ReactNode } from 'react'

class Home extends React.Component {
  render(): ReactNode {
    return <Card title={'Attachment'}>asdf</Card>
  }
}

export default Home
